#include <stdio.h>

/**
 * main - print all single digit number in base 10
 *
 * Return: Always 0 (sucess)
 */
int main(void)
{
	int i;
	for (i = 0; i < 10; i++)

	{
		printf("%i", i);
	}

	printf("\n");

	return (0);
}
